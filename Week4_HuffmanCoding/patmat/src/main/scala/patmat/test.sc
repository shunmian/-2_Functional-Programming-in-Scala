package patmat

object test {
      val chars = "abccc".toList
    val tr                                        //> Welcome to the Scala worksheetee = createCodeTree(chars)
    println(tree)
    val encodeBits = encode(tree)(chars)
}